class ApplicationController < ActionController::Base
  protect_from_forgery

#protected
  #def initialize_session
  #  session[:cart] ||= []
   # @my_cart = []
  #  session[:cart].each { |id| @my_cart << Products.find(id) }
  #end
end
